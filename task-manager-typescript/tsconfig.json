{
  "compilerOptions": {
    "target": "ES2020" /* Especifica la versión de JavaScript para la compilación. */,
    "module": "commonjs" /* Sistema de módulos de Node.js (`require` y `module.exports`). */,
    "outDir": "./dist" /* Carpeta de salida para los archivos compilados. */,
    "rootDir": "./src" /* Carpeta donde se encuentran los archivos TypeScript. */,
    "strict": true /* Habilita todas las verificaciones estrictas para mayor seguridad. */,
    "esModuleInterop": true /* Facilita la interoperabilidad entre CommonJS y ESModules. */,
    "forceConsistentCasingInFileNames": true /* Garantiza el uso consistente de mayúsculas y minúsculas en los nombres de archivos. */,
    "skipLibCheck": true /* Acelera la compilación al omitir la verificación de definiciones de tipos (`.d.ts`). */,
    "moduleResolution": "node" /* Especifica cómo TypeScript busca archivos desde módulos. */,
    "resolveJsonModule": true /* Permite importar archivos JSON directamente. */,
    "noImplicitAny": true /* Genera un error para variables de tipo `any` implícito. */,
    "noImplicitReturns": true /* Muestra errores si algunas rutas de funciones no devuelven un valor. */,
    "noFallthroughCasesInSwitch": true /* Detecta fallthroughs en declaraciones `switch`. */
  },
  "include": ["src"] /* Incluye todos los archivos de la carpeta `src`. */,
  "exclude": [
    "node_modules",
    "dist"
  ] /* Excluye la carpeta `node_modules` y la carpeta de salida `dist`. */
}
